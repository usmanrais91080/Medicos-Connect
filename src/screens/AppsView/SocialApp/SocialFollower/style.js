import { StyleSheet } from "react-native";

export default StyleSheet.create({
    headerRightContainer: {
        flexDirection: "row",
        marginRight: 15,
        alignItems: "center",
    },
    container: {
        flex: 1
    }
})